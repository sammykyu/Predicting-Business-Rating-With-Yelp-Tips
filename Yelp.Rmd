---
title: "Yelp"
author: "Sammy"
date: "October 6, 2015"
output: html_document
---


```{r}
require(RJSONIO)
readJSONFile <- function(fileName) {
  mydata <- fromJSON(fileName)
#   mydata <- lapply(mydata, function(x) {
#     x[sapply(x, is.null)] <- NA
#     unlist(x)
#   })
} 

```

BUSINESS:
```{r}
business <- readJSONFile("yelp_academic_dataset_business.json")
str(business)
df_business <- data.frame(matrix(unlist(business), byrow=T))
str(df_business)
```

Checkin:
```{r}
checkins <- readJSONFile("yelp_academic_dataset_checkin.json")
str(checkins)

```

Review:
```{r}
reviews <- readJSONFile("yelp_academic_dataset_review.json")
str(reviews)

```

Tip:
```{r}
tips <- readJSONFile("yelp_academic_dataset_tip.json")
str(tips)

```

User:
```{r}
users <- readJSONFile("yelp_academic_dataset_user.json")
str(usres)

```


