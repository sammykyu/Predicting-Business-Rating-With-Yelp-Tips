---
title: "Yelp"
author: "Sammy"
date: "October 6, 2015"
output: html_document
---

```{r}
require(RJSONIO)

```


```{r}

processJsonData <- function(jsonData) {
  jsonData <- lapply(jsonData, function(x) {
    #x[sapply(x, is.null)] <- NA
    unlist(x)
  })
  do.call("rbind", jsonData)
} 

```

BUSINESS:
```{r}
bus_ls <- fromJSON("yelp_academic_dataset_business.json")
str(bus_ls)
bus_df <- processJsonData(bus_ls)
str(bus_df)
```

Checkin:
```{r}
checkins <- readJSONFile("yelp_academic_dataset_checkin.json")
str(checkins)

```

Review:
```{r}
reviews <- readJSONFile("yelp_academic_dataset_review.json")
str(reviews)

```

Tip:
```{r}
tips <- readJSONFile("yelp_academic_dataset_tip.json")
str(tips)

```

User:
```{r}
users <- readJSONFile("yelp_academic_dataset_user.json")
str(usres)

```


